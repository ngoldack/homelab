apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: authentik-postgres
  namespace: authentik
spec:
  instances: 1
  storage:
    size: 10Gi
    storageClass: tns-csi-fast-nfs
  bootstrap:
    initdb:
      database: authentik
      owner: authentik
