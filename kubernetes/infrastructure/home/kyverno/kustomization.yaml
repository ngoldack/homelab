apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - sources.yaml
  - helmrelease.yaml
  # Audit-only ClusterPolicies (see the file header for the audit-vs-enforce
  # reasoning and the namespace exclusion scope).
  - policies.yaml
