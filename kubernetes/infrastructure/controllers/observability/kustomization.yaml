apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - namespace.yaml
  - secret.sops.yaml
  # VictoriaMetrics stack: metrics + logs + traces
  - helmrelease-victoriametrics.yaml
  - helmrelease-victorialogs.yaml
  - helmrelease-victoriatraces.yaml
  # Collection
  - helmrelease-otel-collector.yaml
  # Dashboards
  - helmrelease-grafana.yaml
  - dashboards
