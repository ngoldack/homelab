# Falco runtime security controller install. Shared HelmRepository
# (falcosecurity) lives in ../sources.yaml.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - helmrelease.yaml
