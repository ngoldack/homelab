# agentgateway allowlist — the only traffic that may cross the default-deny baseline.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: agentgateway-allowlist
  namespace: agentgateway
spec:
  endpointSelector:
    matchLabels: {}
  ingress:
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: agentgateway
    - fromEntities:
        - ingress
        - health
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: monitoring
    # In-cluster OTLP senders (every app exporting traces/metrics) — OTLP only;
    # the proxy's own listeners are scoped by the rules below, not to every pod.
    - fromEntities:
        - cluster
      toPorts:
        - ports:
            - {port: "4317", protocol: TCP}
            - {port: "4318", protocol: TCP}
    # The Hermes gateway is the only in-cluster :80 client of the proxy
    # (config.yaml: base_url http://agentgateway.agentgateway.svc.cluster.local:80/v1).
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: hermes
      toPorts:
        - ports:
            - {port: "80", protocol: TCP}
  egress:
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: agentgateway
    # Model backends: the only in-cluster backend is the llmkube llama.cpp
    # InferenceService (backends.yaml: qwen36-35b.llmkube-system.svc:8080), so
    # this is scoped to that namespace instead of every cluster endpoint.
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: llmkube-system
      toPorts:
        - ports:
            - {port: "8080", protocol: TCP}
    # JWT validation: the gateway fetches the OIDC provider's JWKS from
    # authentik's server/portal ports in the authentik namespace (observed as
    # a sustained denied flow when this rule was missing).
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: authentik
      toPorts:
        - ports:
            - {port: "9000", protocol: TCP}
            - {port: "9443", protocol: TCP}
    - toEntities:
        - kube-apiserver
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "6443", protocol: TCP}
    - toCIDRSet:
        - cidr: 10.30.0.10/32
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "6443", protocol: TCP}
    - toCIDRSet:
        - cidr: 172.21.0.0/16
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "6443", protocol: TCP}
    # Upstream model providers (OpenRouter etc.) and OTLP export.
    - toEntities:
        - world
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
