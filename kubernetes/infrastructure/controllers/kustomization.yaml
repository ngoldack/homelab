apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Helm chart sources (HelmRepository definitions) consumed by the controllers below
  - sources.yaml
  # Networking & CRDs
  - gateway-api
  - cilium
  # Certificates
  - cert-manager
  # Storage
  - storage/local-path
  - storage/tns-csi
  # Observability
  - observability
  # Security & runtime enforcement
  - security
  - tetragon
  # Networking overlay
  - netbird
  # Data operators
  - cnpg-operator
  - cnpg-barman-plugin
  - valkey-operator
  # AI/ML serving & autoscaling
  - kserve-operator
  - keda
  # Object storage
  - minio-operator
  # Infrastructure-as-data control plane (provisions MinIO buckets/users/policies)
  - crossplane
  # Platform services
  - backup
  - k8sgpt
  - headlamp
