apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Application toggle list: each entry is a directory under kubernetes/apps that
# Flux reconciles. v0 (MVP) ships only llama-cpp (added in the llama.cpp step).
resources:
  - llama-cpp
