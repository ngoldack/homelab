apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-vars
  namespace: flux-system
spec:
  interval: 10m
  path: ./kubernetes/clusters/cloud/cluster-vars
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system