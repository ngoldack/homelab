apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Cluster-wide config that depends on the controllers. Minimal in v0; Cilium
# runtime config (LB-IPAM pool) is added alongside Cilium.
resources: []
