apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Tracing policies are CRs of the cilium.io TracingPolicy CRDs that the
  # tetragon release installs, hence their own Kustomization (see
  # kubernetes/clusters/home/tetragon-policies.yaml).
  - tracingpolicies.yaml
