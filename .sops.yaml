creation_rules:
  # Encrypt all SOPS secret files in the tofu directory
  - path_regex: tofu/secret\.sops\.yaml$
    age: &homelab_age_key >-
      age1h3lgj5t47k5vx67ck9gztwa7kr8pafzlnk725p82nvlvhn8z9g0qze7alf

  # Encrypt all SOPS secret files in the kubernetes directory
  - path_regex: kubernetes/.*\.sops\.yaml$
    age: *homelab_age_key
