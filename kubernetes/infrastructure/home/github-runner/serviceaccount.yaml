# The runner's own identity. The workflow's flux/kubectl calls reach the API
# server with this ServiceAccount's in-cluster token — there is no kubeconfig
# Secret anywhere — and the RBAC that makes those calls useful lives in
# rbac.yaml, scoped to flux-system.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ci-runner
  namespace: github-runner
