# Offsite location (TrueNAS) — v3

OpenTofu for the `offsite` location lands in **v3** (see
`openspec/changes/v3-offsite`): a TrueNAS-hosted Talos cluster (`offsite`,
cluster id 3) for offsite/disaster-recovery, joined to the mesh.

Nothing is provisioned here yet.
