# metrics-server allowlist — the only traffic that may cross default-deny.
#
# Two flows, each with its justification:
#
#   1. INBOUND — the Kubernetes API server routes /apis/metrics.k8s.io/* to
#      this release's APIService (apiService.create: true). Clients
#      (Headlamp, autoscalers) never reach metrics-server directly; they call
#      the apiserver, which proxies to metrics-server:443. On Talos the
#      apiserver is a hostNetwork static pod, so that aggregation connection
#      arrives with the host/remote-node identity — NOT the `kube-apiserver`
#      entity, which Cilium only assigns once it resolves the apiserver as a
#      Service endpoint. Verified live (2026-09-21): every aggregation attempt
#      was logged as `drop (Policy denied) identity remote-node->… :10250 tcp
#      SYN`, which held the APIService at FailedDiscoveryCheck while the pod
#      itself was Ready and scraping normally. The entity + control-plane VIP +
#      Service-CIDR forms below stay for this repo's other API-client clients.
#   2. OUTBOUND — metrics-server reads per-node container resource stats from
#      each kubelet via the node-status port. Traffic to kubelets is the
#      apiserver's proxy path; metrics-server resolves nodes through the API
#      and talks to the kubelet endpoints the same way node-exporter does in
#      ../monitoring/. DNS resolves those node names and the API server.
#
# Health probes from kubelet (the `health` entity) are admitted inbound.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: metrics-server-allowlist
  namespace: metrics-server
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: metrics-server
  ingress:
    # 1. The API server proxying metrics.k8s.io to this APIService.
    - fromEntities:
        - kube-apiserver
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "10250", protocol: TCP}
    # The apiserver's REAL path (see the header note): a hostNetwork static pod
    # whose traffic carries the host/remote-node identity — exactly the pair the
    # registry, authentik and operator namespaces allow for node-originated
    # traffic. The `kube-apiserver` entity above never matches on Talos.
    - fromEntities:
        - host
        - remote-node
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "10250", protocol: TCP}
    - fromCIDRSet:
        - cidr: 10.30.0.10/32
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "10250", protocol: TCP}
    - fromCIDRSet:
        - cidr: 172.21.0.0/16
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "10250", protocol: TCP}
    - fromEntities:
        - health
  egress:
    # 2. Resolve + reach the kubelet/node paths via the apiserver proxy.
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
    - toEntities:
        - kube-apiserver
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "6443", protocol: TCP}
    - toCIDRSet:
        - cidr: 10.30.0.10/32
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "6443", protocol: TCP}
    - toCIDRSet:
        - cidr: 172.21.0.0/16
      toPorts:
        - ports:
            - {port: "443", protocol: TCP}
            - {port: "6443", protocol: TCP}
    # Kubelet monolith: metrics-server scrapes each node's kubelet directly on
    # node-IP:10250 (--kubelet-preferred-address-types=InternalIP,...). The
    # `cluster` entity covers the node/remote-node identities, exactly as the
    # monitoring scrape stack uses it for the same kubelet:10250 reads. Without
    # this rule metrics-server starts Ready but /apis/metrics.k8s.io returns an
    # EMPTY list (default-deny drops the kubelet scrape) — blank CPU column
    # despite a green pod.
    - toEntities:
        - cluster
      toPorts:
        - ports:
            - {port: "10250", protocol: TCP}
